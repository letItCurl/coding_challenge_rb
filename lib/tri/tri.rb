class Tri
    def initialize(arg)
        @arr = arg.dup
    end
    
    def perform
        lmax = @arr.length - 1
        for i in (0..lmax) do
            for j in (0..lmax) do
                
            end
        end
    end
end

f = Tri.new(ARGV)
f.perform